There used to be an RSS feed for new uploads on [the Reaktor User Library](https://userlibrary.native-instruments.com/reaktor) - now there is again.

Feed available [here](http://radian.sdf-eu.org/reaktor_library_rss.php).

I also used to tweet the uploads [here](https://twitter.com/reaktor_lib) but ~I've lost the keys / forgotten the password.~ the fucker who owns twitter now deleted it. 

Run via `runner.sh` (in a detached tmux loop) or cron. No third-party dependencies
required - the script is standard library only.
